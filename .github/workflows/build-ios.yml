name: Create and Build iOS App

on: [push]

jobs:
  create-and-build:
    runs-on: macos-latest
    steps:
      - name: Create Project
        run: |
          # コマンドでプロジェクトを生成する
          mkdir MyNijinApp
          cd MyNijinApp
          # コマンドラインでプロジェクトを作成
          xcodebuild -create-xcframework-dry-run  # これはあくまで例です
          # 実際にはプロジェクトの雛形を生成する必要があります
          
      - name: Build
        run: |
          # ここにビルド処理を書く
